# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# PostgreSQL
DB_USER=dinesafe
DB_PASSWORD=changeme
DB_NAME=dinesafe

# Grafana admin account (applied on first boot only)
# Access the admin console at localhost:3000/grafana/
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=changeme
