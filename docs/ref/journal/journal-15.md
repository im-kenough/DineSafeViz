# Journal 15

## 2026-04-28 00:00
Add LinkedIn and GitHub icon buttons to the right of the nav tabs

- Task: add two social link buttons (LinkedIn, GitHub) to the `.tabs` row, pushed to the right on all three page templates.
- Approach: add a `.social-links` wrapper with `margin-left: auto` in the flex `.tabs` container; style as compact icon-only square buttons matching the existing dark surface aesthetic; add inline SVG icons.
- Files edited: `src/web/static/style.css`, `src/web/templates/index.html`, `src/web/templates/dashboard.html`, `src/web/templates/info.html`.
